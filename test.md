这个题目建议按“先收敛、再切片、后审查”调用。

如果已运行 ./install.sh 并新开了 Codex 会话，第一步输入：

$mvp-delivery

我要实现一个 TypeScript Scheduler：

- 最大并发数为 limit
- add(task, options) 返回 Promise
- 支持优先级
- 支持取消
- 单个任务失败不能影响其他任务

限时 30 分钟。先不要写代码。

请先：
1. 提出必须澄清的问题；不能提问时写出合理假设。
2. 定义最小 API、状态机、验收标准和非目标。
3. 重点说明优先级相同的公平性、运行中取消的语义、取消任务 Promise 的结果、同步抛错/
异步 reject 的处理。
4. 给出最小实现与可扩展实现的取舍，以及按分钟计划。
5. 等待我确认后再写代码。

你应主动向面试官说明的推荐假设是：

> 任务尚未开始时可取消并从队列移除；已开始任务无法被通用地强制中断，只能通过
> AbortSignal 协作取消。优先级高的任务先执行；相同优先级按 FIFO。每个任务的 Promise
> 独立 resolve/reject，调度器在 finally 中释放并发槽位并继续 drain 队列。

确认设计后，第二步：

$feature-slice

按刚确认的 Scheduler API 实现最小垂直切片。先不要写代码。

先列出要创建/修改的文件、核心数据结构和关键不变量：
- activeCount 永远不超过 limit
- 只要有空闲槽位，就继续取最高优先级的未取消任务
- 每个任务无论成功、失败或取消，都不会阻塞后续 drain
- 每个 add 返回的 Promise 只能完成一次

列出测试：并发上限、优先级/FIFO、队列中取消、单任务失败后继续执行、同步抛错。等待我
确认后再实现。

代码完成后：

$engineering-review

请审查当前 Scheduler 实现。优先检查并发槽位是否在 finally 中释放、重复 drain 是否造
成超发、取消与任务启动之间的竞态、Promise 的 settle 次数、同步异常、内存泄漏和测试遗
漏。用“证据 → 影响 → 最小修复”输出；不要重写代码。

如果测试失败或出现竞态，再调用：

$debug-loop

Scheduler 的测试/行为如下：<粘贴现象>。
先不要改代码。请复现并定位 activeCount、队列和 Promise 状态的变化，提出最小验证步
骤；确认根因后再建议修复。

共享屏幕时，你可以边做边说：

> 我先把“取消”的边界定义清楚，因为通用 Promise 无法安全强制终止已运行任务。
> 然后我用 finally 统一释放并发槽位，保证单任务失败只影响自身 Promise，不影响调度循
> 环。