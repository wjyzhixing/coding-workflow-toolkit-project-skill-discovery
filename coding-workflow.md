# AI 辅助交付工作台

这是一份面试指导、日常工作也可复用的工作流。AI 用来加速澄清、初稿和检查遗漏；范围、取舍、审查和交付结论始终由我负责。

## 使用原则

1. 先收敛，再编码：每次先要假设、验收标准和最小范围。
2. 一次只推进一个阶段：计划确认后才要实现；实现后才要审查。
3. 以可验证的垂直切片为单位：优先跑通用户主路径，而非堆砌页面或抽象。
4. 明示取舍：每个关键选择都能说清“为什么现在这样做、什么时候需要升级”。
5. AI 的输出是候选方案：逐段阅读、自己决定、自己验证；不要整段盲贴。
6. 完整引导式交付自动维护记录：范围确认后建立交付记录，在关键决定、改动、验证与风险发生后更新，结束时默认在对话输出任务类型对应的报告。

## 30 分钟交付节奏

| 时间 | 目标 | 可直接说的话 |
| --- | --- | --- |
| 0–4 分钟 | 澄清、假设、验收标准 | “我先把用户主链路、边界和成功标准定清楚。” |
| 4–8 分钟 | 最小方案、数据/API/状态契约 | “我选这个方案，因为它在当前预算内风险最低，也能留下扩展点。” |
| 8–22 分钟 | 实现可运行垂直切片 | “我先完成可验证主路径，再补必要的异常状态。” |
| 22–27 分钟 | 验证边界与错误路径 | “我现在检查 loading、empty、error 和输入/权限边界。” |
| 27–30 分钟 | 总结取舍、下一步与风险 | “若继续投入，我会按用户影响和上线风险优先补这些能力。” |

## 提示词语法

每条请求都尽量包含五件事：

`上下文` + `目标` + `约束` + `交付物` + `决策边界`

例如，不说“帮我做完这个题”，而说“先不要写代码；给出最小范围、验收标准、非目标和计划，等我确认”。这样整个过程既快又可解释。

## 阶段 1：澄清与范围

### React 页面

```text
我需要在 30 分钟内交付一个 React + TypeScript MVP。先不要写代码。

题目：<题目>
已有约束：<仓库、组件库、接口、时间限制>

请：
1. 列出最多四个必须澄清的问题；若不能提问，列出合理假设。
2. 定义最小范围、成功路径、验收标准和非目标。
3. 列出页面需要处理的 loading、empty、error、success 状态。
4. 给出按分钟划分的实施计划。
5. 给出一个更快方案和一个更可扩展方案，说明本题推荐哪个及原因。

保持简洁，等待我确认后再写代码。
```

### React + Node.js 垂直切片

```text
我需要在 30 分钟内交付一个 React + Node.js + TypeScript 的最小垂直切片。先不要写代码。

题目：<题目>
已有约束：<仓库、依赖、数据库、接口限制>

请提出澄清问题或列出显式假设，并定义：成功路径、验收标准、非目标、最小数据模型、API 契约、前端状态、输入校验和授权规则。标明哪些输入必须在服务端验证。

随后给出按分钟计划、最小方案与一个可扩展方案及取舍。等待我确认后再实施。
```

## 阶段 2：实现

```text
按已确认范围实现最小版本。先列出准备修改或创建的文件、每个文件的职责、关键状态和验证方式；等待我确认。

实现时：复用现有项目模式；不引入非必要依赖；不做无关重构；对外部输入做边界校验；显式处理相关的 loading、empty、error 状态。完成后说明如何运行和验证主路径。
```

## 阶段 3：验证与审查

### 高级工程审查

```text
请基于任务目标和当前变更做一次高级工程审查。不要重写代码。

按以下优先级只列出高价值问题：正确性、数据与权限安全、边界/错误处理、可访问性、性能、可观测性、可测试性。

每项使用“证据 → 影响 → 最小修复”格式。若某项没有问题，简要说明检查结论。提出改动前等待我确认。
```

### 证据驱动调试

```text
不要猜测修复。先帮助我复现并记录预期行为、实际行为、环境和最小证据；提出按优先级排序的假设，以及验证每个假设的最小观测方法。

定位根因后，给出最小修复、回归测试和验证命令；等待我确认后才改代码。最后总结根因、修复、验证结果和剩余风险。
```

## 高级追问卡片

| 维度 | 当前 30 分钟取舍 | 继续投入时 | 如何验证 |
| --- | --- | --- | --- |
| 性能 | 优先正确数据流和可感知状态 | 大列表虚拟化、缓存、请求去重、性能指标 | profiler、真实交互和 Web Vitals |
| 可访问性 | 语义元素、可见焦点、表单标签与错误关联 | 键盘流程、屏幕阅读器、自动化审计 | 键盘走查、axe 等工具 |
| 安全 | 服务端验证输入和权限，前端不信任 | 速率限制、审计日志、威胁建模 | 越权/非法输入测试 |
| 可观测性 | 有明确失败信息，不吞异常 | 结构化日志、错误追踪、业务指标 | 模拟失败并检查告警链路 |
| 数据一致性 | 简化模型并明确接口语义 | 幂等、事务、冲突解决、重试策略 | 并发与重复请求测试 |
| 测试 | 主路径 + 关键异常状态 | 单元、集成、端到端分层覆盖 | 自动化回归与覆盖率趋势 |
| 扩展性 | 选择最小清晰边界 | 抽象稳定接口、分页、异步任务 | 新需求接入成本与压测 |

## 中文讲解句式

- “我的假设是……；如果这点不成立，我会先调整……。”
- “我把范围压到……，因为它直接验证了用户价值。”
- “这里我选择 A 而不是 B：A 的实现和验证成本更低，B 会在……时再引入。”
- “AI 帮我生成初稿和检查遗漏；数据流、边界和最终取舍由我确认。”
- “我现在优先验证失败路径，因为主路径通常更容易看起来正确。”
- “这版可以交付；上线前我会优先补……，风险是……。”

## 临时环境回退

没有预装 skills 时，只复制本文件的三个阶段提示词。保持“先范围、再实现、后验证”的节奏即可；不要依赖特定命令、模板或黑盒自动化。
